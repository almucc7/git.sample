/* import minimist from 'minimmist';

const args = minimist(process.argv.slice(2));

switch (args[0]) {
    case 'greet':
        if (args[1] === '-n') {
            console.log(`Hola ${args[2]}`);
            break;
        }
        console.log(`Hola ${args[1]}`);
        break;

    default:
        break;
}

*/